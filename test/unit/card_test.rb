require 'test_helper'

class CardTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end
  
  # a card should always have a frontimg
  
  # a card should have a fingerprint set after create
  
  # a card's fingerprint should not change after update
end
